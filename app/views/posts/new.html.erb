<%= form_with model: [ @user, @user.posts.build ] do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %><br><br>
  <%= form.label :body %>
  <%= form.text_area :body %><br><br>
  <%= form.submit 'Create' %>
<% end %>